---
# Full first-run: bootstrap then provision.
# Re-runs should call provision.yml directly.
- name: Bootstrap debian dev
  ansible.builtin.import_playbook: bootstrap.yml

- name: Provision debian dev
  ansible.builtin.import_playbook: provision.yml
